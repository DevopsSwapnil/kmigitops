apiVersion: v1
kind: Pod
metadata:
  name: simple-pod-001
  namespace: default
labels:
  app: simple
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80